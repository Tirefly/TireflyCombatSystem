# 战斗系统重建设计工作区（文档索引 + 决策日志）

- 阶段：**设计阶段收束（M0–M9 全拍板），进入 R3 实施规划**——R3 构成 A' 六模块定稿（Core/Attribute/Effect/Targeting/Damage/Integration），writing-plans 产出实施计划
- 输入三调研：`tcs-legacy-research/`（TCS 代码级，原 tirefly-combat-system-research）、`ability-kit-research` 技能（AbilityKit）、本目录既有裁决文档
- 工作法：每模块一轮 = 决策点提案 → 用户逐项拍板 → `NN-module-<name>.md` 设计文档 → 检查点纪律
- 本文件是决策日志：拍板一项更新一项（状态 待拍板 → 已拍板：日期+决定+一句话依据）

## 文档清单

| 文档 | 内容 | 状态 |
|---|---|---|
| `2026-09-02-rebuild-module-map-proposal.md` | 模块地图 M0–M8、依赖铁律、顺序方案 A/B/C | **已被 R0 取代（历史存档）** |
| `2026-09-02-r0-rebuild-position-paper.md` | 立场文档：为什么重建、继承/放弃、非目标、验收标准 | v1 已确认 |
| `2026-09-02-m0min-m2-decision-points.md` | M0-min 与 M2 的 12 项（D0×5+D2×7：含 D2-6 值域模式、D2-7 Custom op 砍除） | 已拍板 |
| `2026-09-02-m3-states-decision-points.md` | M3 状态层的 7 个决策点（已按 2026-09-02 证据核验；提前起草，顺序无关） | 已消费 |
| `01-module-m0-core.md` | M0 内核正式设计：句柄/池/总线/时钟/诊断 + 网络姿态接口位 | v1 |
| `02-module-attributes.md` | M2 属性层正式设计：FName 词表/聚合管线/事务/操作复制网络策略 | v1 |
| `03-module-states.md` | M3 状态层正式设计：Def 类层级/中央注册表/五轴堆叠(MaxStacks)/关系表+Cancels/Level/ParamSnapshot/Fragment | v2 |
| `2026-09-02-m4-effects-decision-points.md` | M4 决策点 v2 **已拍板**：触发行 10 字段/事件分类表/原语集 17+3/目标选择每步骤挂载 + Authoring 三层模型（**原语集终版 15 见 D4-16/04 文档；目标选择 v2 策略化见 D4-4 v2/10 文档**） | 已拍板 2026-09-02 |
| `04-module-effects.md` | M4 效果执行层正式设计：15 原语（注册制分派归属）/触发行求值器/异步四唤醒源/Damage 委托化/Authoring 边界/端到端走查 | v2 |
| `2026-09-02-m5-skill-decision-points.md` | M5 决策点 v3 **已拍板**：施法查询契约/冷却策略×时机矩阵/参数任意键+封闭运算/起链解析让渡点/链重定向+参数注入+技能级重定向(D5-9)+形态组(D5-14)+冷却多轨道(D5-15)+冷却/消耗事件(D5-16) | 已拍板 2026-09-02 |
| `05-module-skill.md` | M5 技能/施法层正式设计：SkillDef/账本/冷却矩阵/Cost 矩阵/参数双表+Mode/ParamSnapshot/Instancing/三粒度重定向 | v2 |
| `2026-09-02-m6-integration-decision-points.md` | M6 决策点 **已拍板**：StateTree 组合式/CombatEntity 命名/Mass 移出当前范围/双层引导（GameInstance DefLibrary + World 驱动器）/复制契约/tick 泵唯一驱动 | 已拍板 2026-09-02 |
| `06-module-integration.md` | M6 集成层正式设计：双层引导/CombatEntity 三职责/复制契约草案/未来扩展清单 | v2 |
| `07-module-presentation.md` | M7 表现层正式设计：Cue 引用制/帧末合并/宿主适配接口/死事件防线 | v1 |
| `08-module-editor-tooling.md` | M8 编辑器与工具正式设计：校验器/可视化/Explain/孤儿扫描修复清单 | v1 |
| `09-module-damage.md` | TcsDamage 瞬时流程层正式设计：**流程模板化**（解释器+标准步骤库+官方默认模板+数据步骤 FlowModify/FlowDelegate+每步骤 Conditions）/流程属性黑板（三层值空间）/修改器唯一通道（触发行+ModifyFlow）/消耗型修正器/模板重定向（四粒度让渡）/记录流（零公式零预设） | v3 |
| `2026-09-02-r3-vertical-slice-script.md` | R3 竖切验收剧本 v2：六模块口径（Core/Attribute/Effect/Targeting/Damage/Integration）/7 人工检查点/D0-6 屏显验收信号 | v2 |
| `2026-09-10-param-value-source-decision-points.md` | Parameter 值来源策略体系决策点（PV-0~10）：FTcsParamValue{TInstancedStruct} 取代 FTcsParamScalar（D2-12 修订）/等级表参数进引擎（D3-11 修订：State/Instigator × Array/Map 四源归 TcsState，ITcsEntityLevelProvider 定义于 TcsState）/D7-2 收窄（基础伤害值=参数账本解算输入、流程零计算、delegate=逃生口）/基类虚函数 Evaluate、Literal+ParamRef 内置 TcsCore、AttributeScaled 归 Attribute；Live 与 Formula 为已承诺基建；PV-10 源的可枚举能力（2026-09-14） | 已全部拍板（已折入） |
| `2026-09-14-param-fold-and-display-decision-points.md` | 参数折叠与展示决策点：**D5-5 v3**（参数链 = 带式聚合，五带同 M2、顺序无关、折叠器单份住 TcsAttribute 三处共用）/ **D5-17 v2→v3**（v2 词法族 → v3 描述视图策略化：视图=结构化策略配置 `FTcsParamView`、StringTable 只含槽名、探针校验、展示政策归开发者）/ **D5-18 v3**（约定可配白名单 + 链行约定列 + Literal 书写值定性）/ **PV-10**（源可枚举能力）/ **PV-1 增补**（上下文补齐 + Evaluate 转发 + 句柄边界让步）/ 命名批（v3 增补视图族） | 已拍板（2026-09-14 / 09-15 增补） |
| `deferred-inputs-ledger.md` | **跨轮遗留输入台账**：登记「决策已拍板、代码未落地/未验证」且不在任何现有计划 Task 里的输入（R4×2 / M0 总线×1 / R6×1 / R7×2 / R8×5 / 触发条件×4）。含入册判据（三条全中）与生命周期纪律（轮次开工通读 → 折进该轮计划 → 收束勾销、不删条目） | 活文档（2026-09-18 建立） |

## 决策日志

### 模块物化（2026-09-02 增补拍板；M9 追问轮对齐 D4-14）
- 编译层十一模块：TcsCore/TcsNotation/TcsAttribute/TcsEffect(**纯机制层**)/TcsDamage/TcsTargeting/TcsState(含 Buff 子域)/TcsSkill/TcsCue/TcsIntegration/TcsEditor。**D4-14 依赖翻转：TcsEffect 只依赖 Core/Attribute，领域步骤类型+执行器归领域模块并自注册（UE_DEFINE_EFFECT_STEP_EXECUTOR）**——Damage/Heal/ModifyFlow→TcsDamage、SelectTargets→TcsTargeting、ApplyState→TcsState、PlayCue→TcsCue；依赖链 `Core←Attribute←Effect←{Damage,Targeting,State}←Skill`（**层级序=方向许可，非强制边**；TcsSkill 编译只取 Core/Attribute/State/Effect——链步骤经注册表分派，编排层不具名领域步骤；组合在 TcsIntegration/宿主）。2026-09-02 增补 TcsDamage（瞬时流程骨架+公式归项目 delegate）与 TcsTargeting（D4-15）；UBT 强制层级依赖；FSourceHandle 归 TcsCore（用户最满意设计，系统级通用归属机制）；TcsCue=插件默认表现适配+经验容器；Mass 移出当前范围（未来 TcsMass）。基础设施内置规定：总线/池内置 TcsCore，不用 Lyra GameplayMessageRouter 替换（rule of two 再议提炼）；宿主出站桥自建。**本块为历史摘要**——现行物化规定以 R0 §9 为准（TcsNotation 为第十一模块等后续增补见该表与决策日志后续条目）。详见 R0 §9。

### 已定（历史裁决，编号继承，不重议）

| 编号 | 决定 | 来源 |
|---|---|---|
| 裁决1 | 状态约束 = Tag 关系表 `FUnitStatusRule{Status,Blocks,Requires,Priority}` | 2026-08-31 关系文档 |
| 裁决2a | 事件 = 总线 + 订阅表 + 共享 Handler UClass；struct 不自绑 delegate | 2026-08-31 事件文档 |
| 裁决2b | tick = TickableWorldSubsystem 泵 ScaledDt | 2026-08-31 tick 文档 |
| 裁决3 | 可视化跟随数据形状（表/矩阵/管线图，不做通用图编辑器） | 2026-08-31 可视化文档 |
| 载体 | 三层分拆：定义 UObject DefAsset / 实例 USTRUCT+句柄+池 / 执行集中 tick；两级单位（Mass 小兵+全功能军官） | carrier 讨论 |
| 聚合 | 修饰符 recalc 式：Override(0)>Add(10)>PercentAdd(15)>Mul(20)、组内顺序无关、脏标记、1e-5 | AbilityKit 对照 |
| 定位 | StateTree 只做决策/编排，不做 buff/skill 执行 | unreal-state-tree 调研 |

### 待拍板（2026-09-02 提交——**本节为历史存档：所列各项均已拍板**，状态列见各行）

| ID | 事项 | 推荐 | 状态 |
|---|---|---|---|
| MD-1 | 模块地图 M0–M8（M4 拆 M4a/b/c 三子模块） | 认可 | **已拍板 2026-09-02** |
| MD-2 | 实施顺序 | C：叶先行（M0-min→M2→竖切→M3→M4→M5→M6→M7/M8） | **已拍板 2026-09-02** |
| MD-3 | TCS 处置：冻结仓库 + 两个悬空 OpenSpec 提案（state-fragment / tcs-turn）冻结，Fragment 思路迁 M3 重审 | 按推荐执行 | **已拍板 2026-09-02** |
| D0-1 | 确定性纪律 | C：轻量纪律 + **网络姿态补充**（服务器权威/headless/镜像/观感预测） | **已拍板 2026-09-02** |
| D0-2 | 句柄形态 | B：索引+代际强类型句柄（净新增，无 TCS 先例，明示） | **已拍板 2026-09-02** |
| D0-3 | 事件载荷类型安全 | C：核心事件用具体 FStruct + FInstancedStruct 扩展位 | **已拍板 2026-09-02** |
| D0-4 | 池与线程模型 | B：单游戏线程假设 + 断言 | **已拍板 2026-09-02** |
| D0-5 | 时钟泵 | B：单一 TickableWorldSubsystem 泵 + FCombatClock（时间源可注入） | **已拍板 2026-09-02** |
| D2-1 | 属性 ID 表示 | FName 词表（项目侧）+ 显式包装结构 + 加载期校验 + 内部稠密 id（封闭枚举出局，TCS 核心选择被继承） | **已拍板 2026-09-02** |
| D2-2 | Modifier 生命周期 | B：M2 无计时器，来源句柄注销→级联移除（TCS 同款，已核验） | **已拍板 2026-09-02** |
| D2-3 | 属性间依赖 | B：读即登记 + Kahn/Tarjan 环检测（TCS 同款；解耦轮数上限与环判定） | **已拍板 2026-09-02** |
| D2-4 | clamp 位置 | B：管线末端统一 clamp，边界三态可选（None/Static/Dynamic 属性） | **已拍板 2026-09-02** |
| D2-5 | 事务与 flush | B：单次管线事务 + 提交尾行内 flush（TCS 同款）；帧末安全点保留默认关；读旧值+PeekPending | **已拍板 2026-09-02** |
| D3-1 | 状态实例存储 | A：中央注册表（per-unit 句柄桶），两级单位同一实现（AbilityKit 同构已取证） | **已拍板 2026-09-02** |
| D3-2 | 定义就绪语义 | B：显式状态机（Ready 必须全量成功，Failed 带清单可重试） | **已拍板 2026-09-02** |
| D3-3 | 关系表执行时机 | B：apply 拒绝 + 移除级联重评（事件驱动，非每帧） | **已拍板 2026-09-02** |
| D3-4 | 堆叠/共存 | 修正：正交轴组合式 `FStateStackPolicy`（GroupBy/MaxStacks（原 Capacity，用户拍板回归 Stack 词汇）/Overflow/ValueStack/StackDurationPolicy 五轴） + **Custom 逃逸位**（值 1），删 Merger 类 | **已拍板 2026-09-02** |
| D3-5 | buff 行为载体 | B：原语链（用户确认此前已定：弃每实例内嵌 StateTree，未来编辑=离散事件图表）；FEffectStep/FEffectChain 已向用户解释 | **已拍板 2026-09-02** |
| D3-6 | 到期驱动 | B：集中到期最小堆，M0 时钟泵驱动（到期才出队） | **已拍板 2026-09-02** |
| D3-7 | 生命周期扩展点 | 三件套（全阶段总线事件+六组数据枚举+Fragment）+ **Custom 逃逸位规约**（全局） | **已拍板 2026-09-02** |

### 拍板记录

- **2026-09-02 MD-1**：模块地图 M0–M8 认可，M1 独立存在（问答框拍板）。依据：强类型 key 空间需要明确归属地；M4 拆三因"何时触发/触发后做什么/打谁"天然三问。
- **2026-09-02 MD-2**：实施顺序 = C 叶先行+竖切验收。依据：每步有消费者、每 2-3 步见活系统，防第二系统效应。
- **2026-09-02 MD-3**：TCS 冻结（留作对照与迁移参考），`add-state-fragment-mechanism` 与 `add-tcs-turn-based-extension` 两提案冻结，Fragment 思路迁新系统 M3 重审。
- **2026-09-02 D0-1**：确定性 = C 轻量纪律，并补充**网络姿态**：服务器权威拓扑、模拟核心 headless 可跑（dedicated server）、权威侧/镜像侧双态、客户端预测仅观感层（施法条/动画/即时自体反馈），结算不预测。服务端权威不要求跨机确定性，四条纪律保留帧同步可能性。
- **2026-09-02 D0-2**：实例表示 = 路线 1 维持（USTRUCT+池+中央注册表），句柄 = 索引+代际强类型。网络税 = 复制代理 Actor + 句柄跨端策略。需求依据：千人战场联机（服务器权威）+ 插件须服务多项目。
- **2026-09-02 D0-3/D0-4/D0-5**：C 混合载荷 / B 单游戏线程+断言 / B 单泵+可注入时间源。
- **2026-09-02 NET-1（需求）**：LAC 本作不联机；插件须可扩展至网络同步——服务项目组多项目，下一项目为自创特摄题材第三人称 ARPG（可能共斗模式）。
- **2026-09-02 NET-2（需求）**：千人战场 = 联机、服务器权威、士兵层不做本地预测。
- **2026-09-02 D2-2/D2-3/D2-4/D2-5**：来源句柄级联撤销 / 读即登记+SCC 环检测 / 末端统一 clamp 三态边界 / 单次管线事务+行内 flush+PeekPending。
- **2026-09-02 D2-1（二次修正）**：用户进一步指出 B' 与 TCS FName 方案"没差多少"（常量文件 TCS 也能做；FName 本质是名字表索引，O(1) 比较可作 TMap key）——**采纳，让步成立**：词表 = 项目侧 FName，封闭枚举出局，TCS 的核心选择被继承。剩余真决策收敛为：API 边界收裸 FName vs 显式包装结构（explicit 构造，约 50 行），稠密 id 降级为内部实现细节（非决策）；引用校验补齐到加载期为硬要求（TCS 现状齐备度待核验，缺则补）。
- **2026-09-02 D2-1（终定）**：词表 = 项目侧 FName（继承 TCS 核心选择）；API 边界用显式包装结构（explicit 构造，裸 FName 传不进属性 API）；引用校验补齐到加载期；稠密 id 为内部实现细节。用户两次质疑（多项目词汇扩展性、"与 TCS 差不多"）均成立并被采纳，封闭 UEnum 方案废弃。
- **2026-09-02 D3-1**：中央注册表定案（用户采纳）。佐证：AbilityKit 取证——生命周期由全局服务管理（DefaultContinuousManager，World DI 战斗作用域），组件为薄适配器；逻辑层零线程原语，防竞态靠单线程执行纪律+显式交互 API（与我方 D0-4 断言强化一致）。
- **2026-09-02 D3-4**：堆叠/共存 = 正交轴组合式 FStateStackPolicy（GroupBy/Capacity/Overflow/ValueStack/Duration 五轴）+ Custom 逃逸位；Merger 策略类删除。
- **2026-09-02 D3-7**：三件套（全阶段总线事件 + 六组数据枚举 + Fragment 机制）+ Custom 逃逸位规约（全局）。
- **2026-09-02 规约（用户原创，全局采纳）——Custom 逃逸位**：系统内每个策略枚举含 Custom 值；选中即暴露 Fragment 类型 + FInstancedStruct Payload 配置；运行时 Fragment 只做决策、引擎执行并守护不变式（post-validate）；两层扩展深度 = 决策 Fragment（顶掉内置轴行为）/ 行为 Fragment（订阅生命周期事件）。**槽位约定：Custom 恒为值 1，值 0 恒为该轴默认内置/None**（用户修正：Custom 不放末位——防后续新增内置值致旧配置失效；Custom 不做默认值——故不入 0 值位；UE 按值序列化本身安全，此为防御性约定）。
- **里程碑：20/20 决策全部拍板完成（2026-09-02）**。**设计阶段 M0–M8 全部定稿（2026-09-02）**：R0 v1、决策点×5（M0M2/M3/M4/M5/M6 全部已拍板）、模块设计文档×8、竖切剧本、README 决策日志。下一步 = 实施路线图（writing-plans）或按需细化。历史里程碑：03-module-states.md 已产出（2026-09-02）；下一步：M4 决策点（核验 09 报告后出 FEffectStep 原语集+触发行 12 字段拍板清单）（FEffectStep 原语集与触发行 12 字段正式拍板在 M4 轮）。
- **2026-09-02 D5-15（M5 追问轮）**：冷却多轨道泛化（D5-3 v2）——`FCooldownPolicy{Tracks:[{Duration, GroupTag?}]}`，Strategy 枚举溶解进轨道（无组=自身轨/有组=单位级组槽，可并存），**真 GCD=纯数据**；用户抓出"不内置职业/公共冷却概念"原句写过头（SharedGroup 本就是机制）——改写为"不内置职业自动归类等游戏专属概念"。依据：单轨道 Entry 存不下自身 CD+公共 CD 并存；GAS 冷却 GE 天然多轨。
- **2026-09-02 D5-16（M5 追问轮）**：冷却三事件 `OnCooldownStarted/OnCooldownUpdated/OnCooldownEnded` + `OnCostApplied`（用户提案）——撤回"冷却结束不发广播"（总线订阅制无噪音税）；配套 `AdjustCooldown/ResetCooldown` API（事件与可变性成对）；CanAfford 失败不发 CostApplied。
- **2026-09-02 D5-5 v2（M5 追问轮）**：BoolSwitch 命名统一——`FBoolSwitchModifier{SwitchKey}`/`IsSwitchSet`；用户抓出"表名避让 Gate、修改器名未跟"的失步（无特殊考虑，编辑失步）；M4 `GateCheck` 保留。
- **2026-09-02 M9 追问轮（依赖方向失步修复，用户发起）**：用户质疑"TcsDamage 不该被 TcsEffect 依赖，应是 TcsDamage 依赖 TcsEffect 并自带 ApplyDamage/ApplyHeal 步骤"——**方向本身早已拍板（D4-14 注册制分派+依赖层级反转），但当轮只落了检查点卡**，M4 决策文档/04/09/R0 §9/03/05/07 七处全部漏折（04 §2.1 残留 17 原语与 Spawn、09 §1 残留"被依赖 TcsEffect"、R0 §9 缺 TcsTargeting 行、03 残留"不依赖 TcsEffect"声明）。本轮全量对齐：R0 §9 升十模块、依赖边缘按 D4-14 链修正、原语归属标注（TcsEffect 本模块 9+领域自注册 6）。步骤命名维持 Damage/Heal（词汇已定稿）。教训与 MEM-20260902-11 同类且更重：折叠验证范围必须含**依赖边缘与计数**，不只命名。
- **2026-09-02 M9 追问轮 2（TcsSkill→TcsDamage 边收窄，用户质疑）**：用户追问"为什么 TcsSkill 还要依赖 TcsDamage"——审查结论：**不需要**。上轮同步时把 D4-14 层级链的"方向许可"误写成强制编译边。TcsSkill 最小编译集 = Core/Attribute/State/Effect（起链走 Effect、Cost/AttrCapture 走 M2、FStateDefBase 继承+关系字段走 State；链步骤 FInstancedStruct 运行时经注册表分派，编排层不具名 FStepDamage）。R0 §9 表注新增"表列依赖=最小编译集，依赖链=方向许可"；组合点=TcsIntegration/宿主。留存这条边会迫使纯增益/位移技能项目拖伤害模块——违背 D4-14 自己的"不需要的项目不引入"。
- **2026-09-02 M9 追问轮 3（TcsDamage 流程管线宿主化，D7-5/6/7）**：用户审 FDamageFlowContext 五问 → 三项拍板：①**D7-5 流程模板化**——用户否定预设阶段表（"Damage 阶段具体都有什么，不同项目有不同项目的说法"）：流程=数据模板 FCombatFlowTemplate，TcsDamage 降为纯机制层，标准十阶段降级为步骤库+官方默认模板；通用数据步骤 FlowModify/FlowDelegate + 每步骤 Conditions（D4-5 复用）→ 编辑器拼流程零 C++，C++ 只入口新语义/新公式；澄清三层值空间（技能参数/公式参数初值 Map/流程属性黑板=契约键名+项目键自由无注册表）。②**D7-6 修改器唯一通道**——伤害修改器=触发行订阅收集事件+ModifyFlow（无独立载体，D3-5 推论）；用户"伤害修改器 vs 流程属性"之惑解答：通道与黑板分层。③**D7-7 模板重定向**——用户拍板一步到位（否决 YAGNI 缓增补）：FFlowRedirect，三粒度让渡升**四粒度**（参数→链→技能→流程）。
- **2026-09-02 M9 收尾轮（R3 启动）**：设计阶段收束。拍板：①**R3 构成 A' 六模块**（Core/Attribute/Effect/Targeting/Damage/Integration；M3/M5/M7/M8 全出——用户质疑"第一阶段验证特效为了什么"后 TcsCue 移出，验收信号改走屏显）；②**D0-6 诊断日志**（三轮演化定稿：注册+级别门面 → 砍自建级别 → **撤销设施**——用户探讨"统一注册入口有无必要"后拍板：日志归 UE 原生分类制（`LogTcs<模块名>` 命名规范）、屏显归测试装置直调 UE API、TcsCore 零日志设施）；③**剧本 v2 重写**（修正四处被打旧：UEnum→FName、TcsTargeting 归属、骨架→流程模板、buff 验证改测试 Source/链数据）。遗留带入 R3：06/03 注册表命名统一（实现细节）。检查点卡 MEM-20260902-01 暂不关闭（跨阶段断点，R3 计划产出后再收束）。命名规范：实现类型按 unreal-cpp-style 加 Tcs 前缀（FTcsAttributeId 等）——设计文档类型名为概念名，R3 执行以计划为准。
- **2026-09-02 R3 规划轮**：①实施阵地定稿——克隆 `TireflyCombatSystem.git` 分支 **remake** 至 `E:\Projects_Dev\LegendAutoChess\Plugins\Tirefly\`（LAC 项目内插件形态；后续 TCS 5.8 重构开发都在此仓库；取代此前"master 分支"口径）；②计划拆两份（计划一 Core+Attribute / 计划二 Effect+Targeting+Damage+Integration）——各自独立可运行；③UE 引擎路径执行期按 unreal-cpp-compile 技能探测。
- **2026-09-02 R3 计划审阅轮**：①计划一 Task 0 简化（remake 已清空，仅 .uplugin）；②实现命名按 unreal-cpp-style **Tcs 前缀**全量落计划（FTcsAttributeId/UTcsAttributeSubsystem/TTcsInstanceHandle 式；枚举值缩写前缀；设计文档类型名为概念名）；③模块布局**平铺**（无 Public/Private，模块类文件与 Build.cs 同级，领域子目录 PascalCase）——unreal-cpp-style 补「模块目录布局」章节（TireflyAgentSkills_Unreal 68a39cb）+ lesson 卡 MEM-20260902-15；④**D0-6 撤销设施**（统一注册入口探讨后拍板：日志归 UE 原生分类制 LogTcs<模块名>、屏显归测试装置直调 UE API、TcsCore 零日志设施——三轮演化：注册+级别门面→砍自建级别→撤销）；⑤**事件总线 BP/CS 反射面 A'（Lyra GMS 形态）**——router 内泛化动态多播 `(EventTag, FInstancedStruct)`（2026-09-11 改名自 ActualTag） + `UTcsAsyncAction_ListenForCombatEvent{Tag 过滤, PayloadType 类型匹配}`（绑定即过滤）；C++ Handler 路径不动；K2 强类型引脚/类型化委托留未来增量；用户线索入档：FInstancedStruct 支持反射与网络同步（载荷较大）；验证项两条（BP InstancedStruct 节点面、CS 载荷读取，不阻塞 R3）。
- **2026-09-02 R3 计划审阅轮 2**：①**D2-10 FlatAdd 加带**（用户问"GAS 新增的 FixedAdd"——取证后吸纳：乘后平坦加不受 %/乘缩放、Override 存在时被覆盖、带权 30；命名 `TAO_FlatAdd` 用户定；02 §2.2 残留 Custom 逃逸位旧文同步修正，纯封闭五带）；②计划一 sketch **权威化修正**（用户识破有损压缩——per-side 三态 Bound、ValueDomain 模式、Modifier 完整形状{Handle,Op,Operand,Source,SortKey,Tag}、门面 UWorldSubsystem 非 Tickable、补 ITcsAttributeProvider 契约）；③FTcsAttributeName 命名对齐 02 规格；④**D2-11 AttributeModOperand**（用户问主属性派生——取证 AbilityKit：Magnitude 动态源同构采纳、表达式引擎 667 行 DSL 不采纳；命名 FTcsAttrModOperand 用户定；"1 力量=2 攻击力"= AttackPower 常驻修正器声明式表达，读即登记复用 D2-3，随计划一 Task 4/5 落地）；⑤**Operand 拒绝清单入档 D2-11**（七项候选逐一判决：ContextFloat/TimeDecay/随机/跨域引用/Custom Fragment 均拒——收敛为三不变式纯函数性/域不透明/封闭公式；"% of 其他属性"已被 AttributeScaled 覆盖；统一逃生口=宿主更新 Literal）。
- **2026-09-02 R3 计划审阅轮 3（描述绑定与记法层）**：①**D5-17 描述文本与数值绑定**——用户提案（StringTable 确保文案唯一 + 占位符引用参数 Id）；修正三处：占位符用 FText::Format 原生 `{Attr:X}/{Param:X}` 命名空间语法（弃自创 `\…\` 标记——本地化参数重排白拿）、组装器按参数元数据包富文本样式（翻译者不碰标签）、M8 占位符扫描；取值 = EffectiveDefId 后的 Entry 账本现值（与逻辑同源不同时机）。②**D5-18 ValueConvention**（用户 GAS 扩展经验：Percent/OneMinus/Negate——策划自然语言值→规范值，既管逻辑输入又管 UI 显示；固定组合顺序）；**升格独立模块 TcsNotation**（R0 §9 第十一模块：策划记法与文本绑定层，所有玩家可见/开发者编辑的数值类配置通用；R3 六模块不变）。⑤SkillDef 加 DescriptionTextKey + 参数行 ValueConvention 列；TcsSkill 依赖加 Notation。
- **2026-09-02 R3 计划审阅轮 4（修正器模板与参数覆写——旧 TCS AttrModDef/SkillModDef 覆写问题收编，旧 OpenSpec 大提案诉求由新架构消化）**：①**D2-12 FTcsParamScalar 统一载体**（TcsCore；"字面量|Param"全插件统一类型：03 DurationTime/05 时段冷却/04 链字段 Param()/模板 Operand.Literal；Literal 存规范值，Param 模式仅定义侧合法、物化后账本纯规范值）；②**D3-19 修正器模板化**（用户拍板保留独立资产、纯模板给默认值，沿用旧名 UTcsAttrModDef/UTcsSkillModDef；FStateDefBase.ModifierRows 纯引用不内联；覆写唯一通道=参数传值——模板声明 ParamKey、键空间=施加方参数表，字段寻址机制整个消失；引用处零字段覆写=PatchField 禁令同类理由；apply 物化 Source=状态句柄；物化执行器注册制分派 D4-14 复用；宿主共用 MaterializeModifiers）；③**D5-19 技能侧**（激活期挂载 Source=施法运行句柄——用户拍板；物化上下文=FCastRun.ParamSnapshot；优先级对号取证 TCS 调研 01/04/07：管线排序不迁移/SortKey 承接/Exclusive 休眠池不迁移）；④**CompeteGroup**（用户定 GameplayTag）：M5 参数链读侧竞争组——组内解析值最大者进折叠、账本全量 Source 撤销自动递补（递补语义保留、无休眠池）；"取优先级最高"=Override+SortKey 既有语义；值竞争与优先级竞争可共存；取值 Fragment 不做（用户判定）、参数 clamp 留判定树候补。语义边界声明：覆写发生在施加/激活物化点，运行中变更=重新施加/live 属性维度/宿主 Literal 更新三正交路（D2-11/D3-12 不变）。
- **2026-09-02 R3 计划审阅轮 5（Operand B 形状 + Notation 边补全）**：①**D2-13 Operand 双形状（B 方案，用户拍板）**——定义侧 `FTcsAttrModOperandDef`（Literal: FTcsParamScalar，可声明"吃施加方参数表哪个键+默认值"）/ 运行侧 `FTcsAttrModOperand`（Literal: double 已解析，账本零膨胀）；物化器单点转换（同 ParamSnapshot"解析后副本"模式，非双轨镜像）；②**FTcsParamScalar 落位 TcsCore 确认**（用户拍板）——所有使用者（Effect/State/Skill/模板资产）的公共下界；Notation 不能放（Effect/State 依赖线无 Notation，且语义是数据形状非记法）；③**D5-18 v2 面补全（用户问"BuffDef 如何配置"）**——**BuffDef 需要**：参数行 {Key, Base, ValueConvention} 镜像 SkillDef 形状 + DescriptionTextKey（buff tooltip 刚需，字段 FName 不需边）；UTcsAttrModDef 补约定列；**依赖边补全**：TcsState←Notation、TcsAttribute←Notation（D5-18 原文"各域←Notation"既录口径的补全非翻转——此前仅 SkillDef/SkillModDef 存在；会话摘要"M2 不依赖 Notation"系过度压缩已纠正）；纯机制层（Effect/Damage/Targeting）不加边，"账本不做二次猜测"不变式不变；④**计划一更新**：TcsNotation 骨架壳进 Task 0（Attribute 依赖边的编译前提，内容=枚举+转换助手最小集）、FTcsParamScalar 进 TcsCore、Operand 双形状+UTcsAttrModDef 进 Task 4、Global Constraints 加记法层约束；折入 02/03/05/R0 §9/m0min（D2-13）/m5（D5-18 v2）。
- **2026-09-02 R3 补文档轮（用户要求补全新模块文档，11/11 齐）**：核对十一模块对文档缺漏——补 **`10-module-targeting.md`**（TcsTargeting：D4-4/14/15/16 折入——FTargetSelector 每步骤挂载与 FEntrySelector 边界、FTargetingShape 形状单一来源、ICombatEntityQuery/IRelationResolver 注入、确定性遍历序、O(N) 性能基线与空间加速留未来、Spawn 移除边界）与 **`11-module-notation.md`**（TcsNotation：D5-17/18/18 v2 + D2-12 边界折入——三变换语义、写入点消费点全图（四写入点）、组装器与占位符扫描、与 Core 平级互不依赖、机制层不加边、骨架壳口径）；04 §2.3/05 §2 加交叉指针。映射确认：01 Core / 02 Attribute / 03 State / 04 Effect / 05 Skill / 06 Integration / 07 M7 表现层=TcsCue / 08 M8=TcsEditor / 09 Damage / 10 Targeting / 11 Notation。
- **2026-09-02 目录口径收窄（用户二次指正，审阅轮 5 后修正）**：用户澄清"平铺"指正的真实范围——**仅 Module.h/Module.cpp + Build.cs 平铺模块根，领域代码恢复 Public/Private 分层 + 领域子目录 PascalCase**（此前记录把范围记宽为"模块级平铺无 Public/Private"，技能章节照失真口径写的）。已执行：unreal-cpp-style「模块目录布局」章节重写（根壳三文件 + Public/Private + include 路径：Public 根相对、根上 Module.h 用模块内相对路径）；计划一/计划二 File Structure 全量重写对齐；lesson 15 卡 effectiveness 记录二次指正与范围确认教训（范围性指正落技能前逐项确认适用范围）。技能子模块已提交 951c959。
- **2026-09-02 日志通道规范（用户新增规范，D0-6 v2）**：模块日志分类声明**独立成通道文件**——`Public/<模块名>LogChannel.h`（DECLARE_LOG_CATEGORY_EXTERN）+ `Private/<模块名>LogChannel.cpp`（DEFINE_LOG_CATEGORY），命名 `Tcs<模块名>LogChannel`（模块名=命名空间划分，用户拍板）；分类名不变（LogTcsCore 等）。**动机**：放 Module.h 的话使用 Log 就要 include Module.h——模块壳只留模块生命周期，使用日志 include LogChannel 头。波及：技能「模块目录布局」章节同步、01/m0min D0-6 口径更新（v2）、计划一（Global Constraints/File Structure×3/拆分规则/Task 0）、计划二（Global Constraints/File Structure×4/Task 0）；竖切剧本无 Module.h 引用不动。
- **2026-09-02 目标选择策略化（审阅轮 5 后讨论，用户三拍板，D4-4/D4-15 v2）**：起因 = 用户问"Plan2 提供几个 TargetSelector 还是抽象基类"——具名审计揪出计划二自相矛盾（FTcsStepDamage 内嵌 FTcsTargetSelector 但 TcsDamage 编译集无 Targeting 边）。三拍板：①**策略模式取代枚举**（用户："枚举太具体、不像模组该有的样子"；Filter"需要接口让宿主实现——怎么算存活怎么算敌人是宿主语义"）——UTcsTargetSelectorStrategy/UTcsTargetFilterStrategy 抽象 + EditInlineNew 内嵌配置，默认实现 Self/EventTarget；②**解法 B**——战斗步骤不内嵌 selector、目标消费 Context.Targets，**Context 默认目标初始化=事件目标**（单步链零 SelectTargets 直接消费），D4-4"战斗步骤内嵌"改口，TcsDamage 编译集 {Core,Attribute,Effect} 保持、边消失；③**RadiusArea/FTargetingShape 后置**——竖切剧本实证单体选择已覆盖（v2 测试链 WaitDelay→SelectTargets(单体)→Damage，无技能无范围选择）；连带纠正：04 §9 例二旋风斩是走查预演形态非 R3 验收用例（M5 落地后才可能成真）。落档：10-module-targeting.md v2 重写、plan2（约束行/Task 2 策略化/Task 4 FTcsStepDamage 去 Selector）、m4 D4-4/D4-15 v2 行。
- **2026-09-02 策略形态一元化（审阅轮 5 后，用户提案，D3-7 v2）**：用户提案把全系统策略统一为 **EditInlineNew Instanced UObject** 形态（此前 targeting 已 B，决策/逃逸位 Fragment 为 CDO+Payload）。**自我修正**：此前"逃逸位两字段是结构性要求"论断收回——Custom 选中暴露单个 Instanced 策略对象同样成立，那是习惯不是结构。**配套修订（D3-7 v2）**：FStateInstance 去 FragmentSet 字段（实例零策略/载荷/订阅句柄——池化 struct 纯度+操作复制地基）；行为 Fragment 订阅契约 = 兴趣 Tag 数据列表 + 单一泛化回调 `OnStateEvent(FGameplayTag, FInstancedStruct, Ctx)`（订阅/退订复用 D4-1 Source 级联配对，作者零订阅管理代码）；决策 Fragment 按决策种类各自抽象接口不变；per-instance 扩展状态 = Variables 通道按需（零消费者不预建）。**用户论据入库**："Fragment 属于外接内容，真要废弃也方便，不会影响 TcsState 内部执行流程"——扩展机制契约面窄、内核不依赖，形态可试错；反命题：会被内核依赖的形态决策才需要一次到位。落档：03（Fragment 机制 v3/五轴/Apply/FStateInstance/修订记录）、m3 D3-7 v2、卡片 MEM-20260902-24。
- **2026-09-02 Mass 连续内存备忘（讨论记录，未拍板）**：用户提出 Mass 核心优化策略是连续内存，讨论 TCS 框架基底能否实现五类型（Buff/Skill/CastRun/Entry/Attr/Modifier）的连续访问+动态增删改查。结论：原语已在手（TTcsInstancePool = DOD 存储核，plan1 Task 1）；审计发现现有热路径（按脏属性聚合/到期驱动）无跨单位全量扫描循环，全局 SoA 红利有限且聚合关键对（Attribute+Modifier）现状已按消费方分组连续；推荐目标形态 = 域级全局稠密池+单位级轻索引（混合式，句柄契约零改动）；**时机纪律 = M3 按桶形态实现 → 1000 单位压测 → 数据说话再做池上移平移**。新文档 `2026-09-02-mass-连续内存数据布局备忘.md`（未来优化抽屉，届时 Mass/性能轮决策点引用）。

- **2026-09-10 模块 DevSettings 约定（用户拍板）**：讨论"每模块是否需要自己的 UDeveloperSettings"后拍板**按需生长**——判据：DevSettings 只承载宿主 ini 可改、项目设置可见的插件级配置；TCS 的可变点已由 Def 资产 / UE 原生日志分类 / CVar 瓜分，模拟语义数值（epsilon、带权、公式）按 R0 §8 宪法封闭不可配置，且 DevSettings 每端一份 ini 会踩 D0-1 确定性（服务器/客户端 ini 分叉）。约定三点：①某模块出现第一个真实配置需求时，在该模块内建 `UTcs<模块名>Settings : UDeveloperSettings`，`GetCategoryName` 返回 `"Tcs"` 归拢同分类、SectionName 区分模块——**不为 11 模块预建空壳**（无消费者预建设施，D0-6 撤销同类逻辑）；②TcsCore 现有 `UTcsDeveloperSettings` 空壳保留（建立 "Tcs" 分类锚点职责）；③**红线：进 Settings 的值不得影响模拟语义**——模拟可调量走 Def 或代码常量，观测走 CVar/日志分类。落档：R0 §9 增补 + README 本条；plan2 grep 核验无 DevSettings 预建、无波及。
- **2026-09-10 策略载体切换 FInstancedStruct（D3-7 v3，可行性调研轮）**：用户问"策略模式 EditInlineNew Instanced UObject 换 FInstancedStruct 有无可行性"→ 调研结论：可行，且恰逢零返工窗口（策略 UObject 零实现，plan2 Task 2 未动工）→ 拍板切换（方案 B）。**新形态**：策略 = USTRUCT 反射基类 + C++ 虚函数分派（StateTree 同构——`FStateTreeConditionBase::TestCondition` shipped 先例），Def/步骤以 `TInstancedStruct<Base>` 成员持有；UE5.8 StructUtils 已迁入 CoreUObject，编辑器开箱（StructUtilsEditor 类型 picker + 内嵌展开 + TArray 专用 details，`TInstancedStruct` 自动 BaseStruct 限定）。**动机**：与 D4-14"数据+分派"心智收敛（效果步骤已是数据 struct+执行器注册表）、StateTree/Mass/SmartObjects/Chooser 引擎同构、网络开箱（原生 NetSerialize + Iris `FInstancedStructNetSerializer`）、编译期类型限定强于 UClass picker、GC 无 UObject 开销。**明确代价（用户接受）**：BP 策略扩展通道放弃（R0 §9"蓝图理论可行不承诺"承责；picker 默认排除 UUserDefinedStruct）、编辑器校验钩子挪至 Def 资产 IsDataValid、FInstancedStruct 无内联缓冲（Def 配置态无碍——"实例零策略"拍板不变，策略不进池化实例）。**不回归**：旧 TCS"TSubclassOf 选类+FInstancedStruct 传配置"成对双形态（R0 §2.1 策略类爆炸实证）。落档：10 文档 v3（§2.1/2.2 载体重写+依据+修订记录）、03（五轴 Custom 行/Fragment 机制 v4/修订记录 v2 增补 6）、04 §2.3 策略模式行、R0 §9 TcsTargeting 行、m3 D3-7 v3 行、m4 D4-15 行注记、plan2（Global Constraints 策略载体行/File Structure/Task 2 sketch FTcs 形态）、启动提示词关键纪律、openspec project.md；卡片 MEM-20260902-24 superseded-by MEM-20260910-04。

- **2026-09-10 Parameter 值来源策略体系（用户拍板，决策点文档 `2026-09-10-param-value-source-decision-points.md`）**：走查"攻击力×[5/7.5/10/13.5/18] 随等级"技能配置暴露三处不足（数值配置下放宿主 delegate 过度、FTcsParamScalar 二值载体表达力不足、等级表无处安放）；用户裁定数值配置属通用 TCS 基建、模块化归属 = TcsCore 提供基础/扩展模块自实现。拍板：①**D2-12 修订**——FTcsParamValue{TInstancedStruct<FTcsParamValueSource>} 取代 FTcsParamScalar（形态遵循 D3-7 v3 虚分派，无注册宏），Constant/ParamRef（参数嵌套）内置 TcsCore，最小解析上下文；②**D3-11 修订**——等级表参数进引擎：StateLevelArray/Map→TcsState、InstigatorLevelArray/Map→TcsSkill（索引源待拍板，推荐泛化 AttributeIndexedArray/Map 归 TcsAttribute）；③**D7-2 收窄**——基础伤害值来源=链步骤数据配置（推荐复合运算走 FlowModify 黑板管线）、预设管线=插件机制、delegate=逃生口（09:38 ★主公式条款随之修订）；④TcsAttribute 提供属性值参数源（AttributeScaled）。M2 账本 Operand 封闭不动（D2-11 拒绝清单不波及）；D2-13 账本零膨胀不变式保留。形状细节八项待拍板（PV-1b~PV-8：上下文扩展机制/Fallback 必填/ParamRef 环检测/Snapshot-only/Formula 逃生口 R3 不做等），拍板后折入 01/02/03/05/09/11/R0 §9 与 plan1/plan2——plan1 Task 4 未动工，FTcsParamScalar 壳零返工改造。

- **2026-09-11 PV 系列细节收束（用户逐项拍板，`2026-09-10-param-value-source-decision-points.md` 定稿）**：①基类虚函数命名 **Evaluate**（用户动词纪律：Resolve 仅句柄/Id→对象；解析上下文随名 **FTcsParamEvaluateContext**）；②Constant 更名 **Literal**（与全系统词汇一致）；③ParamRef 链式允许 + **编辑期 DAG 去重**（禁自引用/成环）+ 限同域 + Fallback 必填且**编辑器阶段即校验**；④AttributeScaled 仅 Snapshot；**Live 与读即登记（脏标记）为已承诺基建、随 Live 化实现**；⑤**PV-4 用户否决 AttributeIndexedArray 归 Attribute 与 TcsSkill 归属两案**——改搬旧 TCS **ITcsEntityInterface::GetEntityLevel 入 TcsCore**（宿主可不经 TcsAttribute 承载等级），State/Instigator/Target × Array/Map 六源**全归 TcsState**（State 持有实体引用，Skill 依赖白拿）；⑥**PV-7 用户否决全部三案**——基础伤害值 = **StateParam 参数账本解算**（复合运算由参数链承载：ModifierRows 引用 UTcsSkillModDef{Add,AttributeScaled(AttackPower)}+{Mul,ParamRef(DamageRate)}，operand 已按 PV-6 换 FTcsParamValue），结果输入伤害流程、**流程零基础值计算**；流程中途公式贡献=伤害修改器自实现（D7-6 通道）；⑦Formula/Delegate 与 Live 同为已承诺基建（用途不限于 Damage：条件 buff {formula} 移速例）；⑧消费面全量切换确认。折入执行：01/02/03/05/09/11/R0 §9/m0min + plan1/plan2。

- **2026-09-11 PV 评判轮修订（agent 评判 → 用户逐项裁定）**：①PV-2.a 遗留——ParamRef 在 EffectiveDefId（重定向/形态组）下的解析目标语义未钉死；用户提出"同槽位队列"口径，**全库检索无记录**，待用户给出出处或重述后落档（推荐钉死：ParamRef 按 EffectiveDefId 生效 Def 参数表求值，口径补齐后重新对齐）；②PV-4 收束——TargetLevelArray/Map 暂不提供（四源），`ITcsEntityInterface` 收窄命名 **`ITcsEntityLevelProvider`** 且**定义移至 TcsState**（不进 Core——接口分离+域词汇下沉，未来要 GetEntityX 再立新接口）；③PV-3 快照条目留**源引用位**（Debug+Live 零迁移）；④PV-1.a 源×上下文合法性矩阵列为 **M8 显式交付物**；⑤**PV-9 已拍板**——采纳 `SkillDef.ParamChainRows`（自带参数修正行：无 FEntrySelector、恒作用于本条目、激活物化进本参数链；State/Buff 侧暂不需要）。折入同步：01/03/05/R0 §9/plan1/plan2/project.md/PV 文档。

- **2026-09-11 D5-9 修订（用户拍板：技能级重定向移除）**：三粒度让渡模式收窄——参数级（NumericSkillModifier/BoolSwitchModifier）与链级（链重定向栈）保留；**技能级整体替换不再走 EffectiveDefId 重定向栈，直接换成新 Skill（新学习身份/Entry：Grant/Revoke，ReplaceSkill 链步骤原语列判定树候补）**。动机：Entry↔Def 一对一保证参数键空间纯净——SkillDef 内 ParamRef 同域解析零污染（旧方案下 Entry 按旧 Def 键空间声明的参数修正器在替换后全部悬空）；等级/冷却进度/形态进度不跨替换继承（宿主显式迁移）。连带：EffectiveDefId 收窄为形态解析（D5-14 保留）；PV-2.d 收束（ParamRef 恒解析于自身 DefId 参数表，形态组=当前生效形态 Def；"同槽位队列"议题消解）。落档：05（§3.1/§3.4/§5 + 修订记录 v2 增补 6）、PV 文档 PV-2.d、README 本条。

- **2026-09-11 D5-14 移除 + EffectiveDefId 整体移除（用户拍板）**：澄清 Aatrox Q 类形态技能的正确形态——**不是新 Skill 取代旧 Skill，而是多个 SkillDef 都学习**（各自独立 Entry/等级/冷却），初始只能放 A、放完 A 或条件满足时同一触发逻辑路由到 B、条件不满足退回 A。形态组内建机制（FFormGroup/FCastSequenceState/AdvancePolicy/ResetPolicy）撤除，改为**组合范式**：阶段标识 StateInstance（纯状态标识）+ 关系字段门禁（Require/Block）+ 触发行推进 + 到期堆重置——零新机制。等级/冷却/进度跨形态同步 = 宿主职责（SkillModifier/状态标识）。**EffectiveDefId 概念整体移除**（重定向已撤、形态解析归组合——无消费者）：描述绑定/ParamSnapshot/ParamRef 全部按 Entry 自身 Def；PV-2.d 终版钉死（ParamRef 无条件解析于自身 DefId 参数表，"同槽位队列"议题消解）。落档：05（§2/§3.1/§3.4/§4/§5 + 修订记录 v2 增补 7）、11（组装器取值口径 + 修订记录）、PV 文档 PV-2.d、README 本条。

- **2026-09-14 参数链带式聚合（D5-5 v3，用户发起）**：用户走查"攻击力×[5/7.5/10/13.5/18] 随等级"时抓出**有序链式**的配置地雷——编辑器里 Mul 行写在 Add 行之前，`(0×0.1)+120` 与 `(0+120)×0.1` 结果差十倍且策划无从察觉。拍板：**参数链聚合与 M2 属性修改器完全同式**（"运算符都是一样的"——语义就该相同）：五带 `Add/PercentAdd/Mul/FlatAdd/Override`、组内与跨行**顺序无关**、SortKey 退化为带权、"取优先级最高"=Override 组取最大值；**折叠初值 = 该键参数行的求值结果（无参数行则 0）**；CompeteGroup 选优后**按带**折叠；**折叠器单份住 TcsAttribute**（M2 属性聚合 / M5 参数链 / TcsDamage 流程属性三处共用——三个消费者的编译边都已含 TcsAttribute，零新边）。连带：`AddPct`→`PercentAdd` 命名失步修正；D2-10 的"M5 参数链不跟进（有序链式即可表达）"口径**作废**；`FTcsNumericParamModifier` 补 `ValueConvention` 列（D5-18 v3）。落档：05 §3.4/§9、02 §2.2/§3、m0min-m2 D2-10、m5 D5-5/D5-19、PV 文档 PV-7、README 本条。

- **2026-09-14 描述绑定词法族（D5-17 v2，用户原则）**：用户原则——**"某个 Parameter 在游戏中如何展示不是我该下结论的，TCS 要做的是提供选项让开发者自己选择"**。故记法层提供**词法族**：`{Param:X}` 单值 / `{ParamSeries:X}` 整表+当前档高亮 / `{ParamRange:X}` 区间（在 `LevelBase`/`MaxLevel` 两点各求一次值，**零新能力**——用户 LevelCurve 的"造成 50~270（随等级提升）"表达由此支撑，"（随等级提升）"是文案不是机制）/ `{Attr:X}`。**绑定机制（用户追问"ParamSource 和文本解析器怎么对应上"的答案）**：文本层**永远只认键名**，取值走"内容模块适配器 → Def 参数表按键找行 → 源虚分派（能力探测/Evaluate）"——Notation 只定义**纯数据面回调契约**（`GetValue/GetSeries/GetRange/GetAttribute`，不含 Core 类型），这是与 Core 互不依赖的机制保证。索引口径双处：技能面板 = Entry 当前 EffectiveLevel；实例 tooltip = **该实例快照的 Level**（运行中升级不追溯）。当前格取账本现值、非当前格取表内基准值。Notation 功能面现状登记：`DecomposeFromCanonical`/组装器/扫描接口**已规格化未实现**，随 M3/M5/M8 轮落地（本次复审发现的"承诺与计划失联"缺口）。落档：11 §2.1/§2.2/§3/§4/§6/§9、05 §2、03 §2/§3.6、08 §2/§4、m5 D5-17 行、README 本条。**（2026-09-15 修订：本条的 token 词法载体被 D5-17 v3 取代——见下条；原则、索引口径、绑定链精神、Notation 功能面现状登记均保留）**。

- **2026-09-14 约定可配白名单（D5-18 v3）**：约定列语义收窄为"**本行自己书写的数值**的口径"——✅ `Literal`/表型源可配；❌ `ParamRef` 禁配（读到的已是规范值，再转即二次转换：0.10 再除 100 变 0.001）、`AttributeScaled` 禁配（`Coefficient×属性`乘积语义歧义），M8 强制校验。链步骤数值字段/修正行 Operand 补约定列（PV-6 全量换型把书写面扩大了）；`Literal.Value` 定性为**配置态书写值**（无约定时即规范值）——修正 `TcsParamSource_Literal.h` 注释与 Notation 契约的措辞冲突（本次复审发现项）。落档：11 §2.1/§3、05 §3.4、PV 文档、Literal 头注释、README 本条。

- **2026-09-14 PV-10 源的可枚举能力 + PV-1 增补 + 命名批**：**PV-10**——`FTcsParamEnumerableSource : FTcsParamValueSource`（Core 可选能力基类）+ `Enumerate(Level, OutValues, OutCurrentIndex)`/`GetIndexForLevel`；**索引解析唯一真相在源、`Evaluate` 同源**（防"tooltip 高亮 10%、实打 7.5%"）；能力探测走虚分派**不做中心 switch**；不可枚举源配 `{ParamSeries:X}` = M8 报错；曲线源区间零能力可用、序列表待需求；随 TcsState 等级源同批落地、**不进 plan1 Task 0**。**PV-1 增补**——上下文补 `Subject`(`FCombatEntityHandle`)/`EffectiveLevel`（随等级源同批；当前代码只有 `ParamTable`，属**已记偏差**）；`FTcsParamValue.Evaluate` 便利转发（`IsValid` 兜底 0）；`FCombatEntityHandle` 进 Core 的**边界让步记录**（Core 唯一持有的战斗实体词汇）；PV-8 校验矩阵升级为**四联**（源 × 上下文 × 可配约定 × 可配词法）。**命名批（用户逐项裁定）**：`FTcsNumericParamRow`/`FTcsBoolSwitchRow`/`ETcsParamMode`/`FTcsNumericParamModifier`/`UTcsStateDef`（基类——用户否掉 `UTcsDefAssetBase`：会暗示覆盖 AttrModDef/AttributeDef；对应 `FStateDefBase` 家族）/`UTcsBuffDef`/`UTcsSkillDef`/`FTcsParamSnapshotEntry`（禁 `Resolved*`——动词纪律）/`FTcsParamSnapshot`/`FTcsParamEnumerableSource`。落档：01 §2.1、03 §2/§3、PV 文档 PV-1/PV-8/PV-10、README 本条。**（2026-09-15 修订：序列/token 载体随 D5-17 v3 改视图策略，四联矩阵"可配词法"轴更名"视图兼容"——见下条）**。

- **2026-09-15 描述视图策略化（D5-17 v3，用户重设计）**：用户四点立场——①View 配置在**文本描述侧、有单独的配置空间**（不能配在 ParamSource 上——同一参数在不同文案要用不同形态）；②认同 Kind+FInstancedStruct / 策略形态；③Source×View **不可能完全匹配** + 必须允许**项目自定义专属视图** → 文本内嵌 token（`{ParamSeries:X}` 等）**无法编辑器预警**（错误悬在共享文案、无法定位配置元素），废；④Source×View **多对多**（同一技能简单描述=当前值、复杂描述=全表+高亮）。拍板：**StringTable 回归纯文案（只含槽名，零语法）**；**视图 = USTRUCT 策略基类 `FTcsParamView`（`BuildText`/`IsCompatible` 虚函数）+ `TInstancedStruct` 持有**（D3-7 v3 同构——Kind+FInstancedStruct 与策略形态同物，取后者：零 Kind 维护、灭配对 bug、picker 开箱）；**配置空间 = `Def.Descriptions: TArray<FTcsDescriptionEntry{DescriptionId, TextKey, Views[SlotName, View]}>`**（取代 DescriptionTextKey 单字段）；内置视图 Value/Series/Range/Attribute（v2 词法转世）；数据面 `FTcsViewBuildContext`（瞬时非反射——PV-1 反射禁令只约束序列化面，StateTree TestCondition 先例）+ `FTcsViewProbe`；**编辑器预警四层**（源能力徽章 → IsCompatible 保存期拦截挂配置元素+替换建议 → 槽名交叉扫描 → 运行期只降级——不变式：编辑期必须报错、运行期只许降级）；**宿主自定义视图 = 一步、零公共代码**（扫描器永不改）；v2 的 GetView(Kind,Out) 回调与 FTcsDescriptionSource/SeriesView/RangeView 作废；保留：索引口径双处、当前格账本现值、约定白名单、降级不变式、展示政策归开发者。落档：11 §2.2 重写/§4、05 §2、03 §2/§3.6、08 §2/§4、01 §2.1、m5 D5-17 v3 行、PV 文档 PV-8/PV-10、决策文档 v3 节、README 本条。

- **2026-09-17 Task 4 复评三问（用户发起，TcsAttribute 落地后复评）**：①**属性定义类型缺失**——plan1 Task 4 的类型清单漏了"属性定义"这一件，定义数据被内联成 `DefineAttribute(单位, 属性名, BaseValue, Bounds, ValueDomain)` 的 5 个参数，而设计口径本就是"词表本体 = 项目 DataTable"（02 §2.1 / 竖切剧本"属性表 4 行 = FName 词表 + 显式包装结构 + DataTable 行" / plan2 Task 6"FName 键 + Base + Bounds"）→ 补实现类型 **`FTcsAttributeDef`（`USTRUCT : FTableRowBase`：`BaseValue`/`Bounds`/`ValueDomain`，行名即属性名，名称不进结构体以避免双真相）**，门面改 **`AddAttribute(Unit, Name, Def)`**（动词纪律：`Define` 易读成"定义词表"，而那是 M8 的事；本 API 的动作是"往单位加一条属性实例"）。②**`UTcsAttrModDef` 基类 `UDataAsset` → `UPrimaryDataAsset`**——查全部 11 篇模块文档 + 决策点文档，`PrimaryDataAsset` **零命中**（原基类只是 plan1 写下、从未被论证）；判据：Def 的引用语义是"FName Id + 注册表/DefLibrary 解析"（03 命名批 `ModifierRows` 行仅存 `TemplateId`；06 `ResolveDef(FName)`/`LoadAll`），主资产身份让该解析与按类型发现/加载归引擎 → **定为 Def 资产族统一约定**（`UTcsStateDef` 家族 / `UTcsAttrModDef` / `UTcsSkillModDef` 同此基类；族内混用两套基类会让 DefLibrary 发现逻辑分叉）。③**词表装载/注册仍归 M8**（`FAttributeRegistry`：`Resolve(FName) → 稠密 id`、重名/非法引用加载期报错、行名↔常量映射校验、DevSettings 指路 DataTable）——零消费者不预建，R3 由调用方显式传定义行（plan2 Task 6 建的那张属性 DataTable 在 R3 只验"能被反射承载"）。落档：02 §2.1/§2.2、03 §2、08 §5、README 本条、plan1 Task 4（文件清单 + sketch + 复评注记）、openspec 提案 `add-tcsattribute-types-and-store`（attribute-types/attribute-store delta + design.md「复评定案」）。

- **2026-09-17 文件名去类型前缀（用户发起，二次复评）**：用户指出 `UTcsAttrModDef` 的头文件/实现文件应为 `TcsAttrModDef.h/.cpp`（UE 规范：**文件名 = 类型名去掉前缀字母**，适用于 A/U/F/T/I）。核查发现该违规**是 plan1/plan2 的 File Structure 系统性写下的**（`UTcsClockSubsystem.h` / `FTcsEventBus.h` / `TTcsInstancePool.h` / `ITcsTimeSource.h` 一路如此），存量覆盖 Task 0-3 与新增文件共 **35 个**。处置：全插件改名（Public/Private 成对）+ 全库 `#include` 与 `.generated.h` 同步（UHT 按头文件名生成产物）+ 活动文档路径清零（含**尚未创建**的 plan2 全部文件与 Task 5 的 `TcsAttributePipeline.h`——不改则后续任务照旧名再犯）+ 全量编译验证（零警告，UHT 产物按新名重生）；归档提案目录保持原貌（冻结历史）。规范落点：`unreal-cpp-style`（structure.md 新增「文件命名」节 + 检查清单项；同时修正该技能检查清单里"模块目录布局为平铺"与 structure.md 收窄口径的自相矛盾）与 `openspec/specs/cpp-module-structure`（本提案 ADDED 需求）。

- **2026-09-17 Def 双轨制语义澄清与形态收口（用户口径，三次/四次复评；**全 Def 族适用**）**：用户对属性定义预期有 `PrimaryDataAsset`——澄清其既定策略：**DataTable 供策划编辑（编辑器阶段，批量编辑方便），运行期一律用 DataAsset**（"切 DataAsset 比 DataTable 的扩展性好，万一后面要给 AttributeDef 添加 Fragment"）。据此：①**形态收口**——定义字段的唯一声明处是**表行**（`FTcsAttributeDefTableRow`：`DefId` + Base/边界/值域），资产 `UTcsAttributeDef : UPrimaryDataAsset` **组合持有一行**（字段集单份），两类型同住一个文件；载荷层 `FTcsAttributeDef` 删除。②**修正器模板同款**——新增 `FTcsAttrModDefTableRow`（`TemplateId` + 模板字段），`UTcsAttrModDef` 改为 `TemplateId` + 行；**局限在案**：模板行含 `FTcsParamValue`（`TInstancedStruct`）列，CSV/Excel 往返丢该列，只支持编辑器内表格编辑。③**调用面收口**——单位侧只认属性名：`AddAttribute(单位, 属性名)` / `RemoveAttribute(单位, 属性名)`，**定义解析是门面内部流程**（门面持属性定义表，宿主/DefLibrary 经 `RegisterAttributeDef` 登记）；`RemoveAttribute` = 整条属性下线（实例连同槽位内容丢弃；来源级联撤销仍走 `RemoveBySource`）。④**候选能力 AttributeSet（用户提出，未裁决不入 R3）**——不同游戏模式下同一 CombatEntity 类可能需要不同属性集合；候选 `UTcsAttributeSetAsset : UPrimaryDataAsset`（`TArray<FName> DefIds`）+ 施加 = 遍历 `AddAttribute`，**选集粒度与施加时机待定**（02 §2.1 候选能力块）。⑤**文件职责整理**——`TcsAttrModInstance.h` 拆出值域词汇：`TcsAttributeBounds.h`（边界三态 + 值域模式，一对概念同文件），原文件只留运算带/运算数双形状/账本修正器。族级回写：02 §2.1/§2.2、03 §2、08 §5；落档 plan1 Task 4 注记、openspec 提案（attribute-types/attribute-store delta + design.md 复评定案 9-13）。

- **2026-09-17 命名标准两则（用户提议/定案）**：①**修正器族命名归位**——`TcsAttributeModifier.h` → `TcsAttrModInstance.h`，族内类型统一短前缀（`FTcsAttrModInstance` / `FTcsAttrModOperand(Def)`），与模板侧 `UTcsAttrModDef` 同族；`Def（模板）↔ Instance（按模板物化出的账本条目）`成对可读。②**Def 命名标准 = `<族>Def`（定义资产，不带 `Asset` 后缀）+ `<族>DefTableRow`（表行）**——用户指出资产侧不齐（`UTcsAttributeDefAsset` 带 Asset vs `UTcsAttrModDef` 不带）并要求定标准；判据：设计概念词本就是"Def 资产"，`…DefAsset` 属语义重复。执行：`UTcsAttributeDefAsset` → `UTcsAttributeDef`（含文件 `TcsAttributeDef.h`/`.cpp`）；文档侧未实现的 `UTcsStateDefAsset`/`UTcsBuffDefAsset`/`UTcsSkillDefAsset` → `UTcsStateDef`/`UTcsBuffDef`/`UTcsSkillDef`（2026-09-14 命名批的那三个名字按新标准更新，该决策文档已加修订注记）；`UTcsAttrModDef`/`UTcsSkillModDef` 本就不带 Asset，未动。标准条文落 `openspec/project.md`；落档 plan1 注记、提案 design.md 决策 15/16。

- **2026-09-17 属性存在性与 AttributeSet 裁决（D2-14 / D2-15，用户拍板；决策文档 `2026-09-17-attribute-set-and-existence-decision-points.md`）**：①**D2-14 属性存在性——能力与推荐用法分开书写**：**框架能力 = 允许**运行期 `AddAttribute`/`RemoveAttribute`（"我无法预测其他开发者在自己的项目里如何操作"——不替使用者设限）；**推荐用法 = 结构**（roster 由 Set 在初始化/重置时确定，运行期只动 modifier）；**不做溯源**（实例不加来源字段 ⇒ 不承诺动态增删的精确撤销；属性下线时槽位内 modifier 一并丢弃，来源级联仍走 `RemoveBySource`，二者不互相替代）；**AttributeSet 与运行期动态集合分离**——Set 只负责初始化/重置。②**D2-15 AttributeSet 形态（B1a+B2+B3a）**：Set = `UTcsAttributeSetAsset : UPrimaryDataAsset`，内容 `TArray<FName> DefIds`（覆写列首版不做）；**粒度 = 实体侧引用**（引擎不认识"游戏模式"轴，换情景 = 宿主换引用）；**生命周期 = 资产 GameInstance 级（归 DefLibrary 管辖面）/ "当前 Set" World 级**（依据 MEM-20260902-05 分层判据）；**施加时序 = `RegisterEntity` 之后且穿过 DefLibrary Ready 门禁之后**；**切换 = diff 替换**（共有保留实例，不打断在飞 modifier、不丢 `SetBaseValue`）+ `ClearAttributeSet` 整组清空（在飞 modifier 随之失效）。③**姿态补充**：属性**集合**变更本期不在复制面内（推荐用法下只发生在注册路径，两端各自确定性重建）；联网项目若运行期动态增删属性，需把"属性增删"作为可选操作类型加进操作流与 D6-4 契约载荷表（留位不实现）。④**落地时机**：AttributeSet 随 **M6 轮**（依赖 Def 资产按类型发现/加载 + 实体注册路径）；D2-14 的姿态可立即回写（无代码改动）。落档：02 §2.2a/§2.1/§4/§5、06 §2.1/§2.2/§3 + 修订记录 v3、08 §5、README 本条；plan1 Task 4 候选块升级为正式裁决指针。

- **2026-09-18 AttributeSet/属性存在性 追加裁决（D2-14 修订 + 冻结兜底；设计定稿、代码待落地）**：用户提出并选定**"冻结/解冻"兜底**——`RemoveAttribute` 改为**把整条属性实例搬进暂存区**（基础值/边界/值域模式/修正器槽位全保留，输出日志），`AddAttribute` **解冻优先**（暂存区有同名则整条搬回、基础值取回冻结前的值，否则新建）。agent 复核建议并被采纳：冻结单位取**整条实例**而非"只冻效果"——代码量相同，但顺带解决"装备穿脱丢等级加成"，且读侧语义与"删除"一致（不引入"停用"第二状态）。**必须同批加一条硬规则**：`RemoveBySource` 的扫描面 MUST 含暂存区（否则来源在冻结期结束时修正器永久滞留、属性恢复后凭空多出数值——比丢数值更难查）。**代价变化**：原代价②（槽位内容被丢弃）与③（重加丢基础值）**已消除**；代价①（"两个来源都要同一条属性时只能整条删"）仍存在但后果降级为"暂时消失、效果冻着、加回即恢复"。**不做**：暂存区上限/TTL/解冻时校验来源死活（框架判断不了——`FTcsSourceHandle` 只是编号无存活性登记）、来源级记账、软删停用状态。**排期 = plan1 Task 5（聚合管线轮）**，与本轮 `RemoveBySource` 同批实现与验证；规格 delta 草稿见决策文档 §附录（Task 5 提案时复制进 specs/attribute-store）。落档：02 §2.2a（含实施状态标注——**R3 Task 4 交付仍是"销毁"语义**）、决策文档 §五（升为裁决 + 代价变化表）、plan1 Task 5 Step 1b、README 本条。

## 检查点状态

- 当前检查点卡：**MEM-20260902-01（task-open，跨阶段断点）**——R3 计划产出后保持开启，执行收束后再关闭（M9 收尾轮拍板）。旧卡 MEM-20260831-01 已收束。

- **2026-09-18 聚合管线与事务落地（plan1 Task 5）**：提案 `add-tcsattribute-pipeline-and-transaction`（18 delta / 2 新能力 `attribute-pipeline`+`attribute-transaction` + 3 修订 `attribute-store`+`attribute-types`+`instance-handle-pool`）。落地内容：①**折叠器单份**（`FoldTcsAttributeBands` 住 Public，五带 + 覆盖带按"优先级 → 同优先级策略 → 有符号值"选赢家并覆盖一切，M5/TcsDamage 复用——D5-5 v3）；②**按需重算与脏标记**（管线是 `CachedCurrent` 唯一生产者）；③**值域收口**（Clamp/Wrap/`AVD_Custom` 逃逸位 ensure + 按 Clamp 回落）；④**读即登记依赖 + Tarjan SCC 成环拒边**（重算轮数上限与环判定解耦）；⑤**变更广播**（epsilon 1e-5，总线立即通道，事件 `FTcsAttributeChangedEvent` + 原生 Tag `Tcs.Event.Attribute.ValueChanged`——命名公约 `Tcs.Event.<域>.<事件名>`；事件语义 = 当前值变了，不是"写操作发生了"）；⑥**事务**（嵌套批 + 唯一提交点 + 隐式批 + `PeekPending` 预览）；⑦**`RemoveBySource`**（扫描面含冻结暂存区）；⑧**冻结/解冻**（`RemoveAttribute` 冻结整条实例、`AddAttribute` 解冻优先、双态约束）；⑨**覆盖带强弱口径**（`OverridePriority` 修正器侧 + `OverrideTieBreak` 属性定义侧四值策略，**不开放自定义**——数值本身不含方向，"取最大值"对越低越强的属性会取到最温和的一条）；⑩**管线可见性口径**（`TcsAttributePipeline.h` 声明移入 `Public/Attribute/`、实现三份 `.cpp` 留 Private——确认未来有跨模块消费者直调驱动求值/事务；**推荐路径仍是门面**、直调为逃生口、实例 MUST NOT 跨帧持有；同仓 TBNS 同款形态）。**六处实施偏差（已回写规格）**：(a) `FTcsCombatEntityHandle` **升格为反射 USTRUCT + `int64 Id`**（事件载荷走总线需反射；UHT 不支持 uint64 属性；副作用正面——PV-1 的上下文 `Subject` 解禁）；(b) 新增 `UTcsAttributeSubsystem::SetBaseValue`（规格"改基值"写操作类的落点，plan1 接口清单原缺）；(c) 成环语义按设计原文"**拒绝该边**"（非整条零写入），另每单位运行时状态（批深度/依赖边）住 `FTcsAttributeStore` 随注销释放；(d) **`AddAttribute` 新建即脏**——原规格写 `bDirty = false`，等于"从定义行抄来的缓存初值"是已结算值，**值域收口被整段跳过**（基础值越界会一直读到越界值）；改为批外读取/提交时结算，**定义与添加顺序不影响结果**，且不在 `AddAttribute` 内立即结算（否则动态边界会读到尚未添加的引用属性 → 把原值钳成 0 且无人再标脏）；(e) **`RemoveBySource` 摘冻结区后标脏该冻结实例**（否则解冻带回已被撤销来源的旧缓存值）；(f) **Wrap 跨度成立条件明文**：两侧边界齐备且 `Max > Min` 才回卷，否则返回聚合原值（确定、不 ensure——"Wrap 没给跨度"属作者侧配置错误，留给 M8 定义校验矩阵）。验证：编译零警告；装置增补管线段（折叠四例 / 广播计数 / 收口四例含 Wrap 退化解 / 依赖传播 / 批内读旧值 + PeekPending + 提交单次广播 / 来源级联含冻结区 / 冻结解冻往返 / 双态约束 / **覆盖带六例 + 端到端四例**）+ 拒绝面三条（`AVD_Custom` / 成环拒边 / 无批提交）+ 模板校验两条（非覆盖带填优先级给警告）；**用户 PIE 实测三轮：首轮 46 通过/6 失败、次轮 51/1（Wrap 夹具）、第三轮 67 条全 PASS（2026-09-18）→ 提案归档为 `2026-09-18-add-tcsattribute-pipeline-and-transaction`，18 delta 并入规格（11 条规格 `validate --strict` 全通过）。**

- **2026-09-18 计划一验收装置落地（plan1 Task 6）**：命令 **`Tcs.Test.Acceptance.Plan1`**（检查点 2/3 + 单位隔离性）+ **`Tcs.Test.Acceptance.Plan1.Dangling`**（检查点 4，opt-in；PIE 中运行），装置 `Source/TcsAttribute/Private/Testing/TcsAcceptanceRig.h/.cpp`（临时件，不入库）。落地内容：①**屏显订阅者** `UTcsTestScreenChangeHandler`——订阅 `Tcs.Event.Attribute.ValueChanged` 立即通道，到达即直调 `GEngine->AddOnScreenDebugMessage`（屏显 = 人工验收信号，TcsCore 零屏显调用纪律保持）+ 计数/留存载荷；②**屏显固定行号排版**（`FScreenSection`，行键固定 → 重跑覆盖同批行，保留 600 秒，同批写 `LogTcsAttribute` 留痕）；③三检查点装置 + 单位隔离性对照（单位 A 主用 / B 隔离 / C 悬空）。**四检查点全绿（用户 PIE 两轮：首轮 3/1 → 修夹具 → 次轮 4/0）**：检查点 2（批内 0 条广播、提交后逐属性各 1 条、来源撤销还原）、检查点 3（`PeekPending` 预览未提交值、提交尾 1 条广播）、检查点 4（悬空句柄写入拒绝 + ensure 命中 + 读取回 0）、隔离性（B 的值不受 A 的修正器影响）。**结论：检查点 2/3/4 在真实世界与真实单位数据上全部通过——R3 剧本的 7 项人工检查单中 2/3/4 已验（1/5/6/7 待计划二 Task 7）。**
  - **语义澄清（检查点 2 的"1 次广播"）**：计划文本的两个 modifier 落在**不同属性**上，而广播逐属性——按"每受影响属性各 1 次重算 + 1 次广播"落地（两属性 = 2 条）；"同属性两次变更只 1 条"由检查点 3 承担，两处合起来才是完整的"不重复重算/重复广播"证明。
  - **落点偏差**：计划写 `Source/TcsIntegration/Testing/`，实际落 `Source/TcsAttribute/Private/Testing/`（该模块归计划二 Task 0 建立；装置只用 M0/M2 能力，非跨模块装置；不为它提前建零消费者的模块壳）。
  - **实测副产品（真实风险，已登记台账 R8-2 / R7-1）**：首轮 FAIL 暴露"**定义按属性名全局共享 + 动态边界引用的属性在某单位缺失 → 值被静默钳到 0**"（实测现场：单位 B 只添加 `Health` 而缺 `MaxHealth`，Health 100→0）。运行期行为符合既定口径（plan1 偏差 5），缺的是**作者侧校验**——归 M8 定义校验矩阵；AttributeSet（M6）侧还需"花名册一致性"（含动态边界引用的属性与被引用属性同集）。
  - **实施提醒（对计划二同样适用）**：unity build 把同模块多个 `.cpp` 合并为一个翻译单元，匿名命名空间的通用符号名会跨文件相撞（本次首编译 `error C2084`：装置内 `MakeLiteralModifier`/`IsClose` 与 Task 5 装置同名，**报错却落在旧装置文件里**）——装置内符号统一加装置前缀。
  - **用户指正后修正（故意 ensure 拆出主命令）**：首轮通过的验收仍带 Error 输出——报错是检查点 4 的**故意** ensure（实现无问题），但它被混进常规验收命令，导致每次跑验收都有 Error 刷屏 + 栈回溯 0.8s + **错误报告上传 1.2s** + 调试器断点风险。这违反既有纪律（`unreal-development-workflow` 引擎机制事实："故意触发 ensure 的检查 MUST 独立成 opt-in 命令"，Task 3 实测沉淀；既有装置也是 `Tcs.Test.Attribute` / `.Dangling` 分列）。已拆为主命令（零故意 ensure）+ `.Dangling`（自带夹具并先声明会命中 ensure）；**复验待办**：两条命令各跑一次。
  - **计划一状态**：**Task 0–6 全部完成**；计划二已开工——**Task 0（四模块骨架）完成**、**Task 1（TcsEffect 链解释器与挂起协议）完成**，下一站 = 计划二 Task 2（TcsTargeting）。

- **2026-09-18 效果链机制层落地（plan2 Task 1）**：提案 **`add-tcseffect-chain-interpreter`**（4 新能力 `effect-chain` / `effect-step-dispatch` / `effect-interpreter` / `entity-query-contract` + `instance-handle-pool` MODIFIED × 2，`openspec validate --strict` 通过）。落地内容：①**链与步骤数据形状**（`FTcsEffectChain{ChainId, Steps, MaxStepsPerFrame=64}` / `FTcsEffectStep`；步骤无公共基类——D4-16）；②**链定义登记表**（`RegisterChain` / `UnregisterChain` / `FindChain`，键 = `ChainId`；重复登记拒、**活动运行态的链拒注销**、`TUniquePtr` 地址稳定持有）；③**执行器注册表 + 一行自注册宏**（`UE_DECLARE/DEFINE_EFFECT_STEP_EXECUTOR`——D4-14 注册制分派落地；注册键 = **步骤 struct 反射类型**；**静态初始化期零 UObject 触达**：只入待解析表、首次 `Find` 才取 `StaticStruct()`，依据引擎 `FNativeGameplayTag::GetIfAllocated()` 同款纪律）；④**解释器与池化运行态**（`ExecuteChain` / `ResumeRun` / `IsRunActive`；运行态持 `ChainId` 不持链指针、每步入器前重解析指针——池元素地址不稳定）；⑤**挂起-恢复协议**（`TSR_Running` 停驻 PC + 到期堆唤醒重入 + 代际校验静默丢弃悬空唤醒 + **挂起零每帧成本**，门面非 Tickable）；⑥**单帧步数熔断**（超 `MaxStepsPerFrame` ensure + 断链）；⑦**`FTcsStepWaitDelay`**（战斗时钟 `Elapsed` 时轴——`slomo` 等比例拉长；缺时钟设施降级不挂死）；⑧**`ITcsEntityQuery` 注入契约**（R3 只声明 `EnumerateEntities`，`GetLocation`/`IsAlive` 随 RadiusArea 轮）+ 门面注入点；⑨**TcsCore 一处修改**：`TTcsInstancePool::Reset()`（确定性清空 + 占用统计随重置回落——避免 `Tcs.Core.PoolSlots` 跨 PIE 会话残留）。
  - **落点偏差**：WaitDelay 执行器住 `Private/Chain/TcsStepWaitDelay.cpp` 自成一文件（计划写 `TcsEffectSubsystem.cpp`）——它是"步骤类型 + 执行器 + 一行宏"最小完整样本；子系统 `.cpp` 保持解释器单一职责。领域代码住 `Public/Chain/`、宿主契约住 `Public/Host/`（`cpp-module-structure` 规格的领域子目录要求）。
  - **顺延（显式交代）**：**Context 默认目标初始化**（D4-4 v2"事件载荷 → `Targets`"）R3 无事件触发源、无法实证 → 随触发行轮（M3/M5）落地，**已入遗留台账**；R3 由调用方预填 `Targets`。**链资产类** `UTcsEffectChainDef` 落点（计划未点名）与 **`ITcsEntityQuery` 的 U 类名 vs Task 5 实现类名撞名**（`UTcsEntityQuery` 已被接口的 U 类占用，实现类须改名）两项已写入 plan2 Task 5/6 交接注记。
  - **验证（2026-09-20 用户 PIE 两条命令全过）**：编译零警告（UHT 首次为 TcsEffect 生成产物）；依赖面 grep 零领域模块（`Source/TcsEffect/` 只 include 自身 + TcsCore）；**`Tcs.Test.Effect.Reject` → `通过 5 / 失败 0`**（3 条预期 ensure + 2 条预期 Error 一条不多一条不少，五处拒绝面各自命中设计中的代码行）+ **`Tcs.Test.Effect` → 通过**（即时 3/0 + 延迟判定块给出 Mark 恰好 1 次、运行态已释放，零 Error）。**装置修复一项**：拒绝面检查 3 留下的挂起运行态改为只含等待步——首轮日志暴露"带 Mark 步时那次补做会跨命令污染主命令计数"（第二次跑拒绝面 ensure 静默、命令秒回时必现）。**副产品观测**：三条故意 ensure 的栈回溯 + 错误上报实测卡顿约 15s，量化了"故意报错必须独立成 opt-in 命令"的理由。
  - **提案已归档**：`openspec/changes/archive/2026-09-20-add-tcseffect-chain-interpreter`（4 新能力 + `instance-handle-pool` 2 条修改并入规格库 → **15 条规格** `validate --strict` 全绿）。

- **2026-09-20 目标选择策略层落地（plan2 Task 2，已完成）**：提案 **`add-tcstargeting-strategies`**（新能力 `targeting-strategy`，《validate --strict》通过）。落地内容：①**选择器策略契约** `FTcsTargetSelectorStrategy`（`Resolve(Context, ITcsEntityQuery*, OutTargets)`）；②**过滤器策略契约** `FTcsTargetFilterStrategy`（`Pass(Candidate, Context)`，**框架零默认 Filter**——存活/敌对是宿主本体论，设计明文）；③**`SelectTargets` 步骤与执行器**——清空 → Resolve → Filters AND（短路、保序）→ 写回 `Context.Targets`，即时步骤恒 `TSR_Completed`；④**跨模块自注册**（TcsTargeting 的步骤喂进 TcsEffect 注册表——D4-14 首个跨模块实证）。
  - **R3 范围收窄（用户拍板）**：**框架零默认选择器**——`FTcsSelSelf` 与 `FTcsSelEventTarget` 都不进 R3（选谁是宿主/内容语义）；EventTarget 依赖的"事件载荷 → 目标"通路随触发行轮（台账 R5-1）。竖切的选择器与过滤器均由测试装置提供。
  - **抽象手法修正（照计划抄会编不过）**：计划写 `Resolve` 用 `PURE_VIRTUAL` 纯虚——该宏在 `CHECK_PUREVIRTUALS` 开启时展开为 `= 0`（`CoreMiscDefines.h:100-102`），USTRUCT 抽象类会撞 UHT 的 `TCppStructOps`（C2259）。落地改为**中性默认实现 + `meta=(Hidden)`**（基类不进 picker；先例 = `FTcsParamValueSource`）。
  - **验证**：编译**零警告**（一次通过）；**机制层零改动自检通过**（`git diff -- Source/TcsEffect` 为空——跨模块注册不需要动 Task 1 的任何一个文件）；依赖面 grep 零越界；**用户 PIE 两条命令均通过**（正路 3 检查 / 边界 2 检查，零 FAIL，边界命令只产预期 Warning）。
  - **归档前规格修正（用户指正）**：初稿曾在规格里写"**R3 范围：框架不提供默认选择器**（MUST NOT）"——与设计文档 10 §2.1"默认实现（框架提供）：Self/EventTarget"冲突，且把"推迟"读成了"取消"。已删除该条，规格只留行为契约；后置事实住 proposal 顺延节 / plan2 注记 / 台账三处。**纪律落档：规格装系统行为真相，"某一轮做到哪儿"归计划注记与台账。**
  - **提案已归档**：`openspec/changes/archive/2026-09-20-add-tcstargeting-strategies`（**3 条需求**并入规格库 → **16 条规格** `validate --strict` 全绿）。
  - **遗留**：`IRelationResolver`（阵营判定契约）设计点名、plan2 无 Task 认领、R3 零消费者 → 入台账 **T-5**。

- **2026-09-20 伤害流程机制层落地（plan2 Task 3，实施完成 / PIE 待跑）**：提案 **`add-tcsdamage-flow-layer`**（新能力 `damage-flow`，6 条需求）。落地内容：①**流程模板与登记表**（`FTcsFlowTemplate`，键 = TemplateId）；②**三层值空间上下文**（`FormulaParams` 只读原料按名取用 / `Blackboard` 工作值 / `FlowSource` 每流程唯一 / `CapturedAttrs` 快照位）；③**流程属性黑板**（`Submit` / `Read` / `Reset`；**`Read` 调共享 `FoldTcsAttributeBands`**——D5-5 v3 三处共用的中间一处；`FTcsConsumePolicy` 只存不裁）；④**流程步骤注册表 + `UE_DEFINE_FLOW_STEP_EXECUTOR`**（与 Effect 侧同构、独立实例、延迟解析）；⑤**同步单帧解释器**（`RunTemplate`，无挂起；中止语义 = 未知类型 / 返回 false → 止于未来）；⑥**收集事件协议**（原生 Tag `Tcs.Event.Damage.*` + 立即通道同步派发 + 上下文指针包装载荷）。
  - **落地收窄（皆在提案钉名表）**：步骤签名钉为 `bool` 中止通道；收集 Tag 改用公约名（设计旧写法 `Combat.Damage.Collect.*` 早于 2026-09-18 公约）；载荷改指针包装（C++ 引用不可反射）；黑板 R3 无值域收口（工作值不是角色属性）；不做 Context 池化 / 模板重定向栈（后者随 M3 状态轮）。
  - **验证**：编译**零警告**（一次通过）；折叠器复用自检通过（TcsDamage 内仅一处折叠调用）；**零改动自检通过**（TcsEffect / TcsTargeting / TcsAttribute / TcsCore 无 diff）；依赖面 grep 零越界；**用户 PIE 两条命令均通过**（正路 3 检查 / 拒绝面 3 检查，2 Error + 1 Warning 均为预期）。
  - **提案已归档**：`openspec/changes/archive/2026-09-20-add-tcsdamage-flow-layer`（能力 `damage-flow` 6 条需求并入规格库）。
  - **台账**：**R6-1 部分勾销**（流程属性黑板侧已消费，剩 M5 参数链一处）；新增 **T-6**（来源发号器应进程唯一）。

- **2026-09-20 链/流程上下文句柄化（用户审阅后拍板，已完成）**：提案 **`switch-combat-contexts-to-entity-handles`** → 归档 `2026-09-20-switch-combat-contexts-to-entity-handles`（`effect-chain` 加"效果链上下文"需求；`entity-query-contract` / `targeting-strategy` / `damage-flow` 三份 MODIFIED；规格库 **17 条** `validate --strict` 全绿）。**动因**：`AActor*` 是计划二自选的（04 §2.1 / 09 §2.1 都没写字段类型），与 06 §33"注册表 Actor 无关性（D3-1）保留为未来前提、Mass 适配**核心零改动**"冲突；10 §2.3 给 `ICombatEntityQuery` 的原文本就是"遍历（**全量句柄**）/ GetLocation / IsAlive"。
  - **改型**：`FTcsEffectContext` / `FTcsDamageFlowContext` 的 `Caster`/`Instigator`/`Attacker`/`Targets` 全改 `FTcsCombatEntityHandle`；`ITcsEntityQuery` 三支能力（遍历吐句柄 + 补 `GetLocation`/`IsAlive`）；目标策略契约与执行器全句柄流转，并**移除框架侧"悬空候选跳过"**（存活是宿主语义——由 Filter 表达，无 Filter 时句柄原样传递）。
  - **两处钉名/纪律**：**句柄 MUST NOT 进内容资产**（运行期发号、跨会话/跨机不同——要指定实体用"稳定标识 FName + 运行期宿主解析"，与 Def 引用规约同款）；**句柄↔Actor 映射唯一归宿主**（装置已给出最小样板）。
  - **过网结构纪律落档**：`openspec/project.md` 新增"过网结构 MUST 纯反射数据——禁 `TFunction`（UHT 报错）、禁 `TMap`/`TSet`（UHT 报错）、`FInstancedStruct` 内层须可复制（M8 校验器兜）"+ 通道建议（追加流 → FastArray / 低频事件 → RPC / 复制属性 = 状态语义）。调研笔记：`Documents/combat-system-design/2026-09-20-replication-posture-research.md`（含未核验清单）。
  - **验证**：编译零警告；**无 Actor 残留自检通过**（三处机制面 Public 零命中）；三套装置命令复跑全过（Effect / Targeting / Damage.Flow 及各自 `.Reject`）。

- **2026-09-21 标准步骤库与 Damage 链原语落地（plan2 Task 4，已完成）**：提案 **`add-tcsdamage-steps-and-primitive`**（新能力 `damage-step-library` 4 条 + `damage-primitive` 3 条）→ 归档 `2026-09-21-add-tcsdamage-steps-and-primitive`。落地内容：①**标准十步**（CollectStart / PreHit / Hit / Crit / Element / BaseDamage / AfterDamage / PreExecute / Execute / Completed——核心四步完整、其余六步最小实现，全部宏自注册）；②**步骤 Conditions 挂点**（`ShouldRunFlowStep` 助手 + `HasAllTags` / `Chance` 两个数据谓词；**Chance 的随机值由调用方注入**——未注入即确定性）；③**两个数据步骤**（`FTcsFlowModify` / `FTcsFlowDelegate`）；④**官方默认模板**（`Default`：四步）；⑤**`FTcsStepDamage` 链原语**（跨模块注册进 TcsEffect 注册表——竖切最后一环）；⑥**`UTcsDamageFlowDelegate`**（五函数全带中性默认实现）；⑦**`FTcsDamageRecord` + 原生 Tag `Tcs.Event.Damage.Recorded` + 环形缓冲**；⑧8 个收集事件 Tag 原生声明。
  - **PIE 三轮全过**：首轮 **3/4**（输入通道错——链侧预写黑板被 `CollectStart.Reset()` 冲掉，改为步骤自提交后又重复计数 10+20=30；默认模板无属性键——插件模板不可能知道项目词表）→ 次轮 **6/1**（契约键不匹配 + 记录 `Final` 一直读废弃键）→ 三轮 **7/0** + 拒绝面 **3/0**。**两轮修复的实质**：**上下文补请求字段**（`BaseDamageInput` / `TargetAttrKey`——输入与目标属"请求"，不随收集重置清除）+ **统一契约键**（伤害量只有 `BaseDamage` 一个键，废除 `FinalDamage`；`Record.Base` = 输入、`Record.Final` = 折叠后的最终值）。**方法论**：三处缺陷全属"设计文档没写到字段级、实现自选通道"——编译期与静态自检全绿，只有 PIE 能照出来。
  - **同批追加：入库调试命令 `Tcs.Damage.DumpRecords`**（提案 `add-damage-record-inspection` → 归档）——回应"策划怎么看 InputDmg 与 FinalDmg 的差别"：打印环形缓冲表格（含 **Input / Final / 差额** 三列，差额 = 修改器净影响），住**正式模块**（第一条入库的调试命令）。逐键归因刻意不做（09 §5 非目标）→ M8 Explain 登记台账 **R8-6**。
  - **验证**：编译**零警告**；跨模块注册自检、依赖面零越界、零公式自检（唯一命中 = 调用宿主 delegate）。**规格库 20/20** `validate --strict` 全绿。
  - **顺延与台账**：**AttrCapture 整套** → **T-7**；`FFlowRedirect` → **R4-3**；`TcsEffect` 剩余 8 原语与 `Heal`/`ModifyFlow` 执行器 → **R5-2 / R5-3**；M8 Explain → **R8-6**（条目总数 **23**）。
  - **Task 6 追加验收项**：定义一个 2–3 步、**完全不用插件默认步骤**的宿主自研流程（验零插件改动 + 零默认步骤依赖，可选地仍落 M2 扣血）。
